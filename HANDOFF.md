# 交接文档 · 成语小书（idiom-stories）

给「接手即改」的同行：本项目是小诗囊 / Maxim 的姊妹篇，定位、技术栈、坑点一脉相承。
以下内容按「改之前必读」组织，照做可避免重复踩坑。

---

## 1. 项目定位（别跑偏）

- 给**阿无自己**用的成语典故阅读小工具。手机优先，微信浮窗/浏览器书签点开即用。
- **不是 App、不做后端、不部署服务器**。能纯前端静态站 + GitHub Pages 解决的，绝不引入后端。
- 内容核心是「白话故事 + 公版古籍原文 + 一句道理」，面向 4 岁以上，随时翻开都能读。
- **数据两层**：前 100 则自写；v4 起扩充 1558 则直接采用台湾教育部《成语典》白话典故，偏「词典体」，经 v5–v7 多轮「规则清理 + 手动改写」已全面达标：正文可读性评分（仅评点题句前正文）6–11 分档（最绕口）残余归零，全部 1558 则评分 <6。

## 2. 硬性改动铁律

| # | 铁律 | 不遵守的后果 |
|---|------|------|
| 1 | 改 `index.html`/`stories.json`/`sw.js` 必须同步升 `APP_VERSION`（`index.html` 常量 + `sw.js` 的 `APP_VERSION`） | 手机微信端长期困在旧版 HTML |
| 2 | 微信内置浏览器禁用 SW（`MicroMessenger` UA 判定不注册） | 微信对 SW 支持差，缓存陷阱多，曾导致作者名/内容不更新 |
| 3 | 隐藏元素用 `v-if` 销毁节点，不要 `visibility:hidden` | 带固定高度的隐藏元素留白 |
| 4 | 白话故事以**公版古籍原文**自写为基准；台湾教育部《成语典》(CC BY-ND) 经主人授权可参考/改写使用，**但禁止**从其他无 License / 来源不明仓库扒取现成故事文本 | 版权风险（见 §3） |

## 3. 数据授权红线（踩过的最大坑）

- 台湾教育部《成语典》是**好 source**：CC BY-ND 3.0 TW = 允许原样分发、允许商业使用，原「禁止改作」。
- **经主人明确授权，该约束已放宽**——其内容可直接参考、改写使用，无需规避。扩充阶段（v4，100→1558 则）即直接采用教育部《成语典》的**白话典故（典故說明）**作为故事文本，授权层面已无障碍；古籍原文仍取自公有领域。
- 实测曾遇到同类陷阱：`jaaack-wang/chinese-fixed-phrases-idioms`（30310 条，故事字段仅 1.5% 有内容，且无 License）、`toobey521/idiom-stories`（975/1000 为模板占位 + 204MB 图，无 License）——**均已弃用**。
- **教训**：评估数据集先算全量分布再看样本，抽样偏差会严重误导（见 dataset-due-diligence 技能）。

## 4. 数据生成流水线（在 `_cydata/`）

```
twdict.parquet (HuggingFace 打包的教育部成语典)
   └─ extract.py  → 清洗(繁→简 opencc / 去 _x000D_ 换行 / 去全角空格)
        └─ pool.json  (5156 条带完整文言原文+白话典故)
             └─ select100.py → selected.json (100 条，出处齐全)
                  └─ my_a/b/c/d.json (自写 story+moral，四批)
                       └─ merge.py → ../idiom-stories/stories.json
```

- `merge.py` 自带两道自检：① 故事字数统计；② 与教育部白话释义的 **4-gram 重合率**，>12% 报警。
- `expand_stories.py` 用于从 pool.json 批量扩充（保留原 100 则，按拼音排序）。
- `rewrite_with_llm.py` 用于把词典体的扩充条目改写成前 100 则的亲子朗读风格（需 `OPENAI_API_KEY`）。
- `score_dictionary_like.py` 给每条故事打「词典体」分数，方便优先改写最绕口的条目。
- `build_tags.py`：给 `stories.json` 每条成语生成 `tags`（主题数组）。策略＝高频成语人工校准表（约 100 条）+ 关键词自动打标（**仅在成语四字本身匹配**，避免正文高频字噪声）+ 低频多字短语在正文精确匹配（扩覆盖）。先备份再落盘；幂等可重复跑。
- **清洗脚本（v5–v7 产出，位于 `_cydata/`）**：`clean_boilerplate.py`（切除句尾「演变套话」+ 补自然点题句）、`clean_boilerplate2.py`（去字源前缀/书证/西元/注音等结构标记）、`clean_boilerplate3.py`（切开头「X原作Y」前缀 + 句尾/句中「亦用于/或指/引申」次义项尾巴）、`zhuyin_to_pinyin.py`（注音符号转拼音）、`rewrite_moral.py`（寓意从「比喻/形容/指…」改写为「这个故事告诉我们：…」）。用法均支持 dry-run（`python xxx.py`）与落盘（`--apply`），且**落盘前务必先备份 `stories.json`**。
- 改数据请走这条流水线，不要手改 `stories.json` 后再忘了对账。

## 5. 关键文件

- `index.html`：单文件 Vue3 应用，UI 为**新中式**（宣纸米黄 + 墨黑 + 朱砂点睛 + 竹青/黛蓝辅色，衬线标题）。当前 **v17**（分享卡底部按钮由「分享转发」改为「复制链接」：点按复制 `?q=成语` 深链，微信内粘贴即达该则；并附可见链接文字可长按选择 + "已复制"轻提示；保存图仍由长按图片）。
  - 顶部主题标签栏：动物 / 人物 / 战争 / 自然 / 学习 / 品德 / 友谊 / 励志 / 智慧 / 历史 / 数字 / 颜色 / 身体 / 言语 / 收藏，点击筛选（来自 `tags`）。
  - 搜索：支持 **中文词 / 拼音全拼 / 首字母**（如 `hldj` → 画龙点睛）。
  - 「目录抽屉」三种模式：按 **拼音**（A–Z，去声调）、按 **出处大类**（经/史/子/集/笔记佛典）、按 **主题**（`tags` 首标签）。
  - 阅读控件：字号（小/中/大）、拼音显隐、**原文显隐（默认隐藏）**、夜读模式、收藏（localStorage）、分享卡（canvas 生成可保存图片，正文为该成语小故事前 100 字 + 省略号；含成语/拼音/标签 + 指向 `?q=成语` 深链的二维码，扫码直达该则。底部按钮「复制链接」复制深链，可在微信内粘贴发送；保存图由长按图片）。卡片支持左右滑动 / 方向键切篇。
  - 印章式 logo（`書` 朱砂方印）+ 翻书淡入动效。
- `sw.js`：透明穿透（不缓存任何资源），激活时强刷所有页面。代价：失去离线能力，但彻底根治「手机端困旧版」。当前 `APP_VERSION='17'`。
- `stories.json`：**1558 则**，字段 `id, word, pinyin, story, source, quote, moral, tags`（tags = 主题数组，由 `build_tags.py` 生成）。
- `build_stories.py`：早期 20 则小样的构建脚本（保留作文风参考，已不再用于生产）。

## 6. 待主人拍板 / 已拍板事项

1. **正式品牌名**：✅ **已定名「成语小书」**（由主人拍板选定；跳出「囊」家族，以「书」呼应主人童年绿皮成语故事词典）。
2. **篇幅策略**：✅ **已拍板——保持当前篇幅**（约 150–320 字，中位约 147 字），不统一拉到 300–400 字。
3. **发布**：✅ **已拍板——发布**。走 `hengjie1993-lang` 账号 + GitHub Pages（参考 poetry-reader / english-quotes 流程），线上地址 `https://hengjie1993-lang.github.io/idiom-stories/`。

## 7. 已验证

- 100 条全部命中、出处齐全；中位典故 ~336 字（源数据维度）。
- 白话故事 4-gram 重合率全部 <12%，原创性干净。
- **目录分组**：拼音 A–Z 共 21 组、出处分 5 类，均 100% 覆盖、无遗漏、无「其他」兜底。
- 内联脚本 `node --check` / `vm.Script` 编译通过，无语法错误。
- 本地 `python -m http.server` 预览、手机同 WiFi 可访问。
- 当前 `APP_VERSION='17'`；已建仓库 `hengjie1993-lang/idiom-stories` 并开 GitHub Pages；数据已从 100 则扩充至 **1558 则**。
- **词典体清洗已完成（v5–v8）**：手动改写累计约 **508 条**（v5 11 + v6 56 + v7 71 + 手动分批 8–19 批 370，按正文评分排名逐批推进，每批 30 条、末批 40 条）；规则清理覆盖其余约 1050 条（去系由组合/`＋`拼接/原作开头/书证/所引典源/注音符号/典源术语，切「亦用于/或指/引申」次义项尾巴，注音转拼音）。**寓意字段 937 条**从「比喻/形容/指…」改写为「这个故事告诉我们：…」。正文可读性评分（score_prose.py，仅评点题句前正文）6–11 档（最绕口）残余已归零，全部 1558 则 <6。
- 全量自检：1558 条零字段缺失；严重词典体残余（系由组合/`＋`/成语就从这里演变/书证/所引典源/原作开头/亦用于/或指/注音/典源/双句号）**全部归零**；247 条含句中自然穿插的词源小注（如「X 原作 Y」），属正常知识性点缀，保留。
- 正文可读性评分（`score_prose.py`，仅评点题句「这就是「X」」之前的「正文」）6–11 分档（最绕口）残余归零，1558 则全部 <6，可直接读给孩子听。
- 遗留：无功能性遗留。可选优化项：① 564 条故事用非「这就是「X」——」格式收尾（仍天然点出成语名，符合「先讲故事最后点成语」），可后续统一；② 26 条「引申」为正常叙事解释，保留。
